#!/bin/sh

DIR="/sys/class/power_supply/max170xx_battery"

# Get the Open Circuit Voltage in millivolts
VOLT=$(( $(cat "$DIR/voltage_ocv") / 1000 ))

# Range (3300mV to 4200mV)
MIN=3300
#MAX=4200

# Percentage: ((Current - Min) * 100) / (Max - Min)
P=$(( (VOLT - MIN) / 9 ))

echo "$P"
